<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.deencompanion.app" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- Prayer Times Widget -->
        <receiver
            android:name=".widgets.PrayerTimesWidget"
            android:exported="true"
            android:label="Prayer Times">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_prayer_info" />
        </receiver>

        <!-- Verse of the Day Widget -->
        <receiver
            android:name=".widgets.VerseWidget"
            android:exported="true"
            android:label="Verse of the Day">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_verse_info" />
        </receiver>

        <!-- Zikr Counter Widget -->
        <receiver
            android:name=".widgets.ZikrWidget"
            android:exported="true"
            android:label="Zikr Counter">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_zikr_info" />
        </receiver>

        <!-- Zikr increment broadcast receiver (handles tap on "+ Count" button) -->
        <receiver
            android:name=".widgets.ZikrIncrementReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.deencompanion.app.ZIKR_INCREMENT" />
            </intent-filter>
        </receiver>

        <!-- Hadith of the Day Widget -->
        <receiver
            android:name=".widgets.HadithWidget"
            android:exported="true"
            android:label="Hadith of the Day">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_hadith_info" />
        </receiver>

        <!-- All Prayers Widget -->
        <receiver
            android:name=".widgets.AllPrayersWidget"
            android:exported="true"
            android:label="Daily Prayers">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_all_prayers_info" />
        </receiver>

        <!-- Hijri Date Widget -->
        <receiver
            android:name=".widgets.HijriDateWidget"
            android:exported="true"
            android:label="Islamic Date">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_hijri_info" />
        </receiver>

        <!-- Charity Widget -->
        <receiver
            android:name=".widgets.CharityWidget"
            android:exported="true"
            android:label="Charity Tracker">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_charity_info" />
        </receiver>

        <!-- Qibla Widget -->
        <receiver
            android:name=".widgets.QiblaWidget"
            android:exported="true"
            android:label="Qibla Direction">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_qibla_info" />
        </receiver>

        <!-- Mosque Widget -->
        <receiver
            android:name=".widgets.MosqueWidget"
            android:exported="true"
            android:label="Nearest Mosque">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_mosque_info" />
        </receiver>

    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
</manifest>
